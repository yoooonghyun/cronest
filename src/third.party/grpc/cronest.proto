syntax = "proto3";

package cronest;

service ScheduleJobService {
  rpc FindOne (HeroById) returns (ReadScheduleJobDto) {}
}

message CreateScheduleJobDto {
  int32 id = 1;
}

message ReadScheduleJobDto {
  int32 id = 1;
  string name = 2;
}